<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="cats">
        <!-- cat -->
        <div class="cat {PARENT_CLASS} {TYPE}"  xpack="{XPACK}" __route="{ROUTE}">
            <div class="icon fa {ICON_CLASS}"></div>
            <div class="content">
                <div class="name">{NAME}</div>
            </div>
        </div>
        <!-- / -->
    </div>

    <div class="cat">
        {CAT}
    </div>

</div>
